<%= form_for(household_user) do |f| %>
  <% if household_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(household_user.errors.count, "error") %> prohibited this household_user from being saved:</h2>

      <ul>
      <% household_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
