<p id="notice"><%= notice %></p>

<%= render "shared/navbars" %>
<div class="container">
  <div class="row">
    <%= render "shared/sidebar" %>

    <div class="col-md-10">
      <div id="midCol" data-spy="affix" data-offset-top="300">

        <div class="well">
          <div class='text-center'>
            <h1><%= @chore.name %></h1>
            <h3>Requested by: <%= @chore.user_id %> | Status: <%= @chore.status %></h3>
          </div>
          <p><%= @chore.description %></p>

          <% if @chore.user_id == current_user.id %>
              <%= link_to 'Edit', edit_chore_path(@chore) %> |
          <% else %>

          <% end %>
          <%= link_to 'Back', chores_path %>
        </div>

      </div>
    </div>

  </div>
</div>


